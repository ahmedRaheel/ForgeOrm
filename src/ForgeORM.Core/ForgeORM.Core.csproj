<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <PackageId>ForgeORM.Core</PackageId>
         <Version>1.3.7</Version>
    <Authors>Raheel Ahmed</Authors>
    <Company>Forge Ecosystem</Company>
    <RepositoryUrl>https://github.com/ahmedraheel/ForgeORM</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <PackageProjectUrl>https://www.developerslabai.com/#forgeorm-full-wiki</PackageProjectUrl>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <PackageReadmeFile>README.md</PackageReadmeFile>
    <PackageIcon>forgeorm-icon.png</PackageIcon>
    <PackageTags>orm;micro-orm;micro-orm;efcore;sql;database;query-builder;bulk;dotnet;forgeorm</PackageTags>
    <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
    <IncludeSymbols>true</IncludeSymbols>
    <SymbolPackageFormat>snupkg</SymbolPackageFormat>
    <PublishRepositoryUrl>true</PublishRepositoryUrl>
    <EmbedUntrackedSources>true</EmbedUntrackedSources>
    <Title>ForgeORM.Core</Title>
    <Description>Core ForgeORM runtime with native ADO.NET SQL execution, repository helpers, transactions, split queries and bulk hooks.</Description>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="..\ForgeORM.Abstractions\ForgeORM.Abstractions.csproj" />
    <ProjectReference Include="..\ForgeORM.QueryAst\ForgeORM.QueryAst.csproj" />
  </ItemGroup>
  <ItemGroup>
    <None Include="..\..\README.md" Pack="true" PackagePath="\" />
    <None Include="..\..\assets\forgeorm-icon.png" Pack="true" PackagePath="\" />
  </ItemGroup>
  <ItemGroup>
    <PackageReference Include="Microsoft.Data.SqlClient" />
  </ItemGroup>

  <ItemGroup>
    <None Include="buildTransitive\$(PackageId).props" Pack="true" PackagePath="buildTransitive\$(PackageId).props" Visible="false" />
  </ItemGroup>

  <Target Name="ForgeORM_BuildAndPackSourceGeneratorAnalyzer" BeforeTargets="_GetPackageFiles;GenerateNuspec">
    <!--
      Pack the source generator into ForgeORM.Core.nupkg so package consumers get
      SourceGenerated mode by only calling:

          options.UseCompilationMode(ForgeOrmCompilationMode.SourceGenerated);

      We build the generator project first, then ask MSBuild for its TargetPath.
      This avoids the broken bin\Debug\ForgeORM.SourceGenerators.dll path and
      the unreliable ReferenceCopyLocalPaths path for analyzer ProjectReferences.
    -->
    <MSBuild Projects="..\ForgeORM.SourceGenerators\ForgeORM.SourceGenerators.csproj"
             Targets="Build"
             Properties="Configuration=$(Configuration);TargetFramework=netstandard2.0" />
    <MSBuild Projects="..\ForgeORM.SourceGenerators\ForgeORM.SourceGenerators.csproj"
             Targets="GetTargetPath"
             Properties="Configuration=$(Configuration);TargetFramework=netstandard2.0">
      <Output TaskParameter="TargetOutputs" ItemName="ForgeORM_SourceGeneratorDll" />
    </MSBuild>
    <Error Condition="'@(ForgeORM_SourceGeneratorDll)' == '' or !Exists('%(ForgeORM_SourceGeneratorDll.Identity)')"
           Text="ForgeORM.SourceGenerators.dll was not built/found. Expected TargetPath from ForgeORM.SourceGenerators GetTargetPath." />
    <ItemGroup>
      <None Include="@(ForgeORM_SourceGeneratorDll)"
            Pack="true"
            PackagePath="analyzers/dotnet/cs"
            Visible="false" />
    </ItemGroup>
    <Message Importance="High" Text="ForgeORM: packing source generator analyzer @(ForgeORM_SourceGeneratorDll)" />
  </Target>
</Project>